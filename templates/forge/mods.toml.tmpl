modLoader = "javafml"
loaderVersion = "[${platform.loader},)"
license = "${project.license}"

[[mods]]
modId = "${project.id}"
version = "${project.version}"
displayName = "${project.name}"
authors = "${project.authors.0}"
description = '''${project.description}'''
